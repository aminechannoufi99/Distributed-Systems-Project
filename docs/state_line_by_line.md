# state.py line by line

Note: blank lines are just for readability.

- `from __future__ import annotations` -> active les annotations de types en avant.
- `import threading` -> pour locks.
- `import time` -> timestamp dans snapshot.
- `from collections import deque` -> file FIFO.
- `from dataclasses import asdict, dataclass` -> dataclass.
- `from typing import Any, Deque, Dict, List, Optional` -> types utilises.
- `# Pizzaioli statuses` -> commentaire.
- `AVAILABLE = "AVAILABLE"` -> pizzaiolo libre.
- `BUSY = "BUSY"` -> pizzaiolo occupe.
- `# Order statuses` -> commentaire.
- `PENDING = "PENDING"` -> commande en attente.
- `PROCESSING = "PROCESSING"` -> commande en cours.
- `COMPLETED = "COMPLETED"` -> commande terminee.
- `@dataclass` -> transforme la classe en dataclass.
- `class Order:` -> structure de donnees commande.
- `    order_id: str` -> id commande.
- `    pizza_type: str` -> type de pizza.
- `    status: str` -> statut.
- `    created_at: float` -> temps creation.
- `    updated_at: float` -> derniere maj.
- `    assigned_to: Optional[int] = None` -> pizzaiolo assigne (ou None).
- `    started_at: Optional[float] = None` -> debut preparation.
- `    completed_at: Optional[float] = None` -> fin preparation.
- `    attempts: int = 0` -> nombre d assignations.
- `    def to_dict(self) -> Dict[str, Any]:` -> convertit en dict.
- `        return asdict(self)` -> conversion automatique.
- `    @staticmethod` -> methode statique.
- `    def from_dict(data: Dict[str, Any]) -> "Order":` -> recree un Order depuis dict.
- `        return Order(**data)` -> construit l objet.
- `class StateStore:` -> etat global central.
- `    def __init__(self) -> None:` -> constructeur.
- `        self.lock = threading.RLock()` -> verrou reentrant.
- `        self.pending_queue: Deque[str] = deque()` -> file FCFS.
- `        self.orders: Dict[str, Order] = {}` -> registre des commandes.
- `        self.pizzaioli: Dict[int, str] = {}` -> statuts pizzaioli.
- `    def initialize(self, pizzaiolo_ids: List[int]) -> None:` -> init des pizzaioli.
- `        with self.lock:` -> protege la section.
- `            if not self.pizzaioli:` -> si pas encore init.
- `                self.pizzaioli = {pid: AVAILABLE for pid in pizzaiolo_ids}` -> tous disponibles.
- `    def snapshot(self) -> Dict[str, Any]:` -> cree un snapshot pour la GUI.
- `        with self.lock:` -> protege la lecture.
- `            return {` -> debut snapshot.
- `                "pending_queue": list(self.pending_queue),` -> copie de la file.
- `                "orders": {oid: order.to_dict() for oid, order in self.orders.items()},` -> commandes en dict.
- `                "pizzaioli": dict(self.pizzaioli),` -> statuts pizzaioli.
- `                "saved_at": time.time(),` -> timestamp snapshot.
- `            }` -> fin snapshot.
